# Depends on:  

name=
version=
release=
source=()

build() {
        cd $name-$version
	        mkdir -pv $PKG/var/lib/scratchpkg/db
        echo "$version release=1" >> $PKG/var/lib/scratchpkg/db/$name

}
